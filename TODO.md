# TODO

- Add MCP to help developers understand the repo and the code;
- Force Git messages to follow same rules for everyone (git lint);
- Gituhb PRs are better than directly pushing code;
- Add automatic Changelog (for version bumping);
- Save the data submitted into the LocalStorage so if user refresh the page, he can start again from where he left;
- Add a message if the user is Offline, so he knows he cannot save the form;
- Add extra github actions;
- Add more tests (unit+e2e);
